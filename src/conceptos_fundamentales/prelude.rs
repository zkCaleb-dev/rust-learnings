use std::collections::HashSet;
use std::prelude::v1::*;

fn main() {

	let mi_estructura: MiEstructura;
	let algo: HashSet<i32>;
	let mapa: std::collections::HashMap<i32,i32>;

	// prelude = Estructuras que ya vienen por defecto en cada aplicacion Rust sin necesidad de llamarlas con use.

	let opcion: Option<i32>;
	let opcion2: std::prelude::v1::Option<String>;

}

struct MiEstructura {

}